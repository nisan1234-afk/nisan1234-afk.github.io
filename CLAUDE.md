# nisan1234-afk.github.io — הערות פרויקט

אתר GitHub Pages סטטי המארח אתרי לימוד/חזרה לבגרות למקצועות שונים, כל אחד בתיקייה נפרדת בשורש הריפו. הקובץ הזה נטען אוטומטית בכל שיחת Claude Code שנוגעת בריפו הזה — אין צורך להעתיק את התוכן שלו ידנית.

## איך לדחוף שינויים ל-GitHub (חשוב!)

**אל תשתמש** בכלי ה-API של GitHub (למשל `mcp__github__create_or_update_file` / `push_files`) לקבצים בינאריים (תמונות, אודיו). כלים אלו מקודדים כל תוכן כטקסט UTF-8 לפני השליחה, ולכן **הורסים בייטים בינאריים** (אומת בבדיקה ישירה — קובץ בינארי קטן שנשלח כך חזר עם בייטים שונים לחלוטין מהמקור).

**הדרך האמינה**: עבודה עם git מקומי רגיל, ודחיפה עם טוקן הסביבה:

```bash
git add <files>
git commit -m "..."
git -c http.extraheader="Authorization: Bearer ${GITHUB_TOKEN}" push origin <branch-name>
```

משתנה הסביבה `GITHUB_TOKEN` קיים בסביבת הריצה של Claude Code ועובד לדחיפה ישירה — אין צורך בטוקן נוסף, הגדרת GitHub App, או אישורי הרשאה מהמשתמש.

כלי ה-API (`create_or_update_file`/`push_files`) כן מתאימים לקבצי **טקסט** (HTML/CSS/MD) בלבד, אם רוצים לעבוד דרכם — אבל git+טוקן עובד לכל סוגי הקבצים ופשוט יותר.

## מבנה הפרויקט

כל מקצוע/כיתה חי בתיקייה נפרדת בשורש הריפו:

```
/
├── tourism-bagrut/          # מגמת תיירות, כיתה יא׳
│   ├── index.html
│   ├── style.css
│   ├── musagim.html, hashivut.html, yam-hamelach.html,
│   │   haamakim.html, mishor-hachof.html, yerushalayim.html
│   ├── podcastim.html       # עמוד פודקאסטים (הטמעות יוטיוב/Drive בלבד, לא קבצים)
│   └── images/
└── (מקצועות עתידיים בתיקיות משלהם, באותה רמה)
```

למקצוע/כיתה חדשים — ליצור תיקייה חדשה באותה רמה, לא לערבב עם `tourism-bagrut/`.

## מדיה (תמונות, אודיו)

- **תמונות**: נשמרות בתוך הריפו, בתיקיית `images/` של כל מקצוע. נדחפות עם git+טוקן (ראה למעלה).
- **אודיו (פודקאסטים)**: **לא** מאוחסן בריפו. מתארח חיצונית (YouTube או קובץ משותף ב-Google Drive) ומוטמע ב-iframe:
  - יוטיוב: תבנית `.video-wrap`/`.video-card` הקיימת ב-CSS.
  - Google Drive: קישור שיתוף עם `/view` בסוף מוחלף ל-`/preview`, מוטמע עם `iframe.drive-audio`.
  - דוגמאות מלאות (כתגובת HTML) נמצאות בתוך `tourism-bagrut/podcastim.html`.

## סגנון עיצוב (RTL, עברית)

`style.css` בכל תיקיית-מקצוע מכיל מערכת עיצוב עקבית: פלטת חול/טורקיז/טרקוטה, פונטים Heebo + Secular One, ורכיבים מוכנים לשימוש חוזר:

- `.goals-box` — תיבת מטרות למידה
- `.term-card` — כרטיס מושג (למילון מונחים)
- `details.qa` — שאלה-תשובה מתקפלת (accordion) לשאלות בגרות
- `.sites-table` — טבלת אתרים/מידע
- `.video-grid` / `.video-card` — רשת הטמעות יוטיוב
- `.podcast-grid` / `.podcast-card` — רשת פרקי פודקאסט (יוטיוב/Drive)
- `.figure` — תמונה עם כיתוב

למקצוע חדש עם אותו לוק — אפשר להעתיק את `style.css` ולהתאים כותרות/תוכן. ל design שונה — לבנות CSS נפרד באותה תיקייה.

## דיוק תוכן — אזהרה חשובה

בהעברת תוכן ארוך (במיוחד עברית, דרך copy/paste לתוך קריאות API) יש סיכון ממשי לאובדן קטעים בטעות (קרה בעבר בפרויקט הזה — חצי מעמוד מושגים נשמט בהעברה ידנית ותוקן רק אחרי השוואת מספר שורות מול המקור המקומי).

**לכן**: כשמעבירים תוכן קיים בין קבצים, עדיף לעבוד על הקבצים המקומיים ישירות (Read/Edit על קבצים בדיסק) ולא "לשחזר מהזיכרון" תוכן ארוך בתוך קריאת כלי. אם כן נדרש שחזור/העתקה ארוכה — כדאי לוודא התאמה (למשל מספר שורות, גודל קובץ, או ל-git blob SHA) מול המקור לפני ואחרי.
